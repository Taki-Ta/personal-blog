# Blog Writing Guide

本规范用于约束本博客后续所有技术文章的写作方式。新增或修改博客文章时，应优先遵循本文。

## 写作定位

博客定位为个人技术博客，面向有一定工程经验的开发者。文章应以解释清楚问题、沉淀实践经验、帮助读者复现或迁移为目标，不写营销式、口号式、百科式或泛泛而谈的内容。

文章应更像一篇经过整理的个人技术笔记，而不是官方文档、课堂讲义或规章制度。可以保留严谨的判断和引用，但表达上应有个人理解、取舍过程和问题推进的痕迹。

文章默认使用中文写作，必要的技术名词、API、命令、类名、方法名和配置项保留英文原文。

## 语气

- 使用冷静、直接、务实的技术表达。
- 少用夸张形容词，例如“极致”“革命性”“史诗级”“一文彻底搞懂”。
- 不用教程平台式开场，例如“大家好，今天我们来学习……”。
- 不使用过度口语化表达，例如“废话不多说”“安排上”“保姆级”。
- 可以表达个人判断，但要给出理由，例如“这里更推荐 A，因为它减少了状态同步成本”。
- 少用命令式和训诫式表达，例如“必须注意”“一定要牢记”“最佳实践如下”。如果需要提醒风险，优先写成具体场景里的取舍。
- 可以使用“我会怎么理解”“这里容易误解”“我觉得更自然的顺序是”这类个人技术笔记式表达，但不要变成闲聊。

## 写作风格

- 不要机械套用“背景、核心结论、详细说明、注意事项、总结”的固定模板。结构应服务于文章主题。
- 开头优先从真实问题、一个具体例子或一次实现里的困惑切入，避免先写一段抽象定义。
- 优先写工程语境中的真实约束、取舍和边界条件。
- 避免只罗列概念，应解释概念之间的关系、使用场景和常见误区。
- 对复杂流程优先使用分步骤说明、表格或代码示例。
- 文章中的每个小节都应服务于主题，不堆砌无关背景。
- 小节标题应自然、具体，避免过多使用“核心结论”“工程注意事项”“最佳实践”这类官方文档式标题。
- 解释算法或机制时，优先写“它为什么出现、解决了什么不舒服的问题、代价是什么”，再展开实现细节。
- 结尾可以总结关键点，但不要写空泛鸡汤。

## 推荐结构

一般技术文章可以参考以下结构，但不要照搬成模板：

```markdown
---
title: 文章标题
date: YYYY-MM-DD HH:mm:ss
categories:
  - 分类
tags:
  - 标签
---

## 背景

说明为什么要写这个问题，以及它在实际工程中的上下文。

## 核心结论

用简短段落或列表说明本文最重要的判断。

## 详细说明

按概念、流程、实现、问题排查等维度展开。

## 示例

给出可运行或接近真实业务的代码、配置或命令。

## 注意事项

说明边界条件、常见坑、版本差异或安全风险。

## 总结

收束全文，列出最值得记住的结论。
```

不要求每篇文章都完整套用以上结构。更推荐根据主题调整成自然的阅读路径，例如：

- 从“普通方案为什么不够用”切入，再引出改进方案。
- 从一次实现或排查过程切入，再沉淀成通用结论。
- 从两个方案的差异切入，再解释各自适用场景。

如果一篇文章读起来像标准答案、产品文档或课堂讲义，应优先改写语气和结构。

## 大致长度

- 短文：800 到 1500 字，适合记录单个问题、命令用法、错误排查。
- 标准文章：1500 到 3000 字，适合解释一个技术点或一个小型方案。
- 长文：3000 到 6000 字，适合系统设计、架构说明、完整流程复盘。
- 超过 6000 字时，应考虑拆成系列文章，除非主题确实需要集中说明。

长度不是硬性指标，但应避免为了凑字数重复解释同一件事。

## 标题规范

- 标题应具体，能表达文章实际内容。
- 避免标题党和过度承诺。
- 推荐标题示例：
  - `OAuth2 双 Token 认证架构设计`
  - `Docker Bridge 网络通信流程说明`
  - `C# 依赖注入容器的基本实现原理`
- 不推荐标题示例：
  - `一文搞懂 OAuth2`
  - `最强 Docker 网络详解`
  - `看完这篇你就会 DI 了`

## Front Matter 规范

每篇文章必须包含：

```yaml
---
title: 文章标题
date: YYYY-MM-DD HH:mm:ss
categories:
  - 一级分类
tags:
  - 标签
---
```

分类建议保持稳定，不要为每篇文章随意创建新分类。标签可以更细，但也应避免同义词重复。

## 代码风格

- 代码块必须标注语言，例如 `csharp`、`python`、`yaml`、`bash`、`json`。
- 示例代码应尽量短小，突出当前小节要说明的重点。
- 不要贴大段与主题无关的完整项目代码。
- 命令行示例使用 `bash` 或 `powershell` 标注。
- 配置文件示例使用对应格式，例如 `yaml`、`json`、`toml`。
- 代码注释应解释意图或关键约束，不解释显而易见的语法。
- 如果代码不可直接运行，应在正文中说明它是伪代码或片段。
- 代码示例前后要说明它想证明什么，不要只把代码贴出来让读者自己猜。
- 对算法类文章，优先给出能体现核心状态变化的短代码；真实工程里的并发、错误处理、配置读取可以在正文中说明取舍。

示例：

```csharp
services.AddScoped<IUserRepository, UserRepository>();
services.AddScoped<IUserService, UserService>();
```

## Markdown 风格

- 使用 `##` 作为正文最高层级标题，不在正文中重复 `# title`。
- 列表用于并列信息，步骤流程使用有序列表。
- 行内代码使用反引号，例如 `access_token`、`docker0`。
- 表格只在对比信息清晰时使用，不强行表格化。
- 图片应有明确用途，避免纯装饰图片。
- 中文和英文、数字之间可适当留空，以提升可读性。

## 技术准确性

- 涉及版本差异时，应写明版本或适用范围。
- 涉及安全、认证、权限、加密、部署时，应明确风险和前提条件。
- 不确定的结论不要写成绝对判断。
- 引用外部资料时，优先引用官方文档或源码。

## 文章修改要求

修改旧文章时，应尽量保持原主题不变。可以优化结构、补充示例和修正错误，但不要顺手改成另一篇文章。

如果新增内容会显著改变文章结论，应在文中说明更新背景，或另写新文章。
